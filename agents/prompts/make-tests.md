# make-tests

Add focused tests for the requested behavior.

Prefer tests that fail before the implementation and pass after the fix. Keep fixtures small. Avoid broad snapshot tests unless they directly protect the behavior.
