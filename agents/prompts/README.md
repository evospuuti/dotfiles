# Prompts

These prompt files are shared source material. The installers can copy selected files into:

- Claude commands: `~/.claude/commands`
- Codex prompts: `~/.codex/prompts`

Use `registry.yaml` as the authoritative list.
