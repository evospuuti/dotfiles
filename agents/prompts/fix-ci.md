# fix-ci

Inspect the failing check, identify the first actionable failure, reproduce locally when possible, fix the root cause, and rerun the relevant verification.

Report:

- failed check name;
- failure cause;
- files changed;
- verification result.
