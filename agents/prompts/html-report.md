# html-report

Create a standalone HTML report when Markdown is too limiting.

Use this for status dashboards, PR review summaries, implementation plans, architecture comparisons, or small interactive artifacts. The report must be self-contained and must not load remote scripts or expose secrets.
