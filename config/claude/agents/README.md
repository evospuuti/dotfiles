# Claude Subagents

These files are templates for Claude Code subagents. They are not installed or invoked by default.

Manual target:

```text
~/.claude/agents
```

Use project subagents for repeatable review, research, planning, and verification tasks. Keep tools narrow and descriptions trigger-focused. A subagent should not own secrets or persistent machine config.
