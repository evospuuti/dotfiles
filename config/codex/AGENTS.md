# Codex Instructions

Work like a careful software engineer.

- Read the relevant files before nontrivial edits.
- Keep changes scoped to the user's request.
- Do not revert user or other-agent work.
- Do not run destructive Git commands without explicit approval.
- Do not commit secrets, tokens, private paths, or local MCP credentials.
- Treat MCP servers as explicit reviewed integrations.
- Verify with the nearest relevant checks before claiming completion.
- Prefer small reusable skills over long always-loaded instructions.
- Use repository `AGENTS.md` files as local context when present.
